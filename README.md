# Demand Analytics Dashboard (React)

This project uses **React.js** on the front end to render a demand analytics dashboard.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

> The page loads React from `esm.sh`, so internet access is required when the browser fetches those modules.
